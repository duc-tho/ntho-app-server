const { verifyUrl, getIdFromUrl, getOriginUrl } = require("../core/url");
const { default: axios } = require("axios");

exports = {
  getHistory: (req, rep) => {
    
  },
  
  saveHistory: (req, rep) => {
    
  }
}